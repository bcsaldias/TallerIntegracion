<p id="notice"><%= notice %></p>

<p>
  <strong>Autor:</strong>
  <%= @comment.author %>
</p>

<p>
  <strong>Comentario:</strong>
  <%= @comment.body %>
</p>

<p>
  <strong>Noticia:</strong>
  <%= @comment.news_item.title %>
</p>

<%= link_to 'Editar', edit_news_item_comment_path(@comment.news_item.id, @comment) %>
<%= link_to 'Volver', news_item_comments_path(@comment.news_item.id, @comment) %>
